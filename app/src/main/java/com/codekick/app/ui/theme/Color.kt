package com.codekick.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark theme (matches web's dark mode: near-black bg, white fg)
val BackgroundDark = Color(0xFF0A0A0A)
val SurfaceDark = Color(0xFF141414)
val CardDark = Color(0xFF1A1A1A)
val ForegroundDark = Color(0xFFF9F9F9)
val MutedDark = Color(0xFF888888)
val BorderDark = Color(0xFF2A2A2A)

// ── Light theme
val BackgroundLight = Color(0xFFF9F9F9)
val SurfaceLight = Color(0xFFFFFFFF)
val CardLight = Color(0xFFFFFFFF)
val ForegroundLight = Color(0xFF0A0A0A)
val MutedLight = Color(0xFF666666)
val BorderLight = Color(0xFFE5E5E5)

// ── Accent colors (from web tailwind config)
val AccentIndigo = Color(0xFF6366F1)     // primary accent
val AccentOrange = Color(0xFFF97316)     // warm accent / streak
val AccentGreen = Color(0xFF22C55E)      // success
val AccentRed = Color(0xFFEF4444)        // destructive

// ── Domain track colors (mirrors Domains.tsx gradient stops)
val DomainCP = Color(0xFF1a1a2e)
val DomainAIML = Color(0xFF2d132c)
val DomainWeb3 = Color(0xFF0f2027)
val DomainWeb2 = Color(0xFF3d1c1c)
